import type { ReactNode } from "react"

type layoutProps = {
    children: ReactNode;
}

const Layout = ({ children }: layoutProps) => {
    <div className="">
        <main>

        </main>
    </div>
}
export default Layout;